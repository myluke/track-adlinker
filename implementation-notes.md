# 实现中的决定与未决事项

## 2026-09-12

- **没有可复用模板源码**：原仓库只有 logo 与范围文档，因此建立最小 SvelteKit 2 / Svelte 5 静态项目。实际工具链要求 Node 20.19+，比初稿的“Node 20 或更高”更精确；推荐 Node 24。
- **正式代理合同仍有应用入口待补**：用户已确认支持邮箱 `support@adlinker.work`、签约主体 `adlinker`、适用法律 `the laws of New South Wales, Australia`、生效日期 `2026-09-12`，并将条款/隐私审定状态设为已生效。法律页满足生效条件；应用域名、登录/注册路径仍为空，不能生成虚构入口。
- **构建与开放分开**：`pnpm build` 可以生成本地可预览的完整静态站；`pnpm release:check` 单独核对正式开放所需配置。目前后者预期失败，需真实资料补齐后运行，不能把构建成功当作已发布或已开通。
- **计费边界**：同一人采用最深阶段，重新互动另计；落地页模式的意向/深度互动单价扣除对应 UV 单价。UV 单价迁到各套餐，三个对话事件单价全局共享。采用 2026-09-12 公开计算器样例回归验证，价格需手动更新；实际事件资格与对话窗口仍以产品账单为准。金额按整数万分位计算，各类别四舍五入到分后求和，仅支持两位小数币种。
- **计算器信息密度**：首屏选择套餐，价格和额度自动带入，开始聊天总人数放在主输入区；深入聊天和重新互动为可选项，当前值在折叠时仍可见。聊天默认量全部改为 0，避免原隐藏默认 1,000 个意向联系人产生 ¥80 超额费用。可选项留空按 0 估算，深入聊天不得超过开始聊天总人数。直跳模式隐藏 UV 额度、访问量及费用明细，切回落地页恢复原访问量；计费函数不变，七语同步。
- **官网产品截图**：首页新增三图切换展示，替代误加的静态后台演示页。使用用户提供的中文截图；邮箱、手机号、联系人及消息内容已在生成 WebP 前以不透明像素遮蔽，仓库只保留脱敏素材。七语翻译展示文案，截图不伪造翻译或业务数据；手机端横向查看，支持独立打开大图。 改为带自动事件提示、脉冲背景轨道和截图切换入场动画的动态展示区。
- **主题偏好**：为实现主题切换，浏览器仅保存 `adlinker-theme`。计算器输入不写入存储、不提交；浏览器仍会向 Cloudflare 发送加载静态文件所需的网络请求。

实际架构、专家建议、配置格式与验证结果见 [实施说明](docs/adlinker-implementation.md)。本次未进行 CI、部署、DNS 修改、提交或推送。
